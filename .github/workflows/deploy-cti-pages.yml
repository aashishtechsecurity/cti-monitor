name: Deploy CTI Dashboard to GitHub Pages

on:
  schedule:
    - cron: '0 */2 * * *' # Every 2 hours
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'cti-monitor/package-lock.json'

      - name: Install Dependencies
        working-directory: ./cti-monitor
        run: npm ci

      - name: Aggregate CTI Feeds
        working-directory: ./cti-monitor
        run: node scripts/generate-cti-feeds.mjs

      - name: Build Web Application
        working-directory: ./cti-monitor
        run: npm run build

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './cti-monitor/dist'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
